-- DROP TABLE IF EXISTS users;
CREATE TABLE IF NOT EXISTS
users(
  id SERIAL PRIMARY KEY NOT NULL,
  image VARCHAR(256),
  name VARCHAR(256) NOT NULL,
  patronus VARCHAR(256) NOT NULL,
  alive VARCHAR(256) NOT NULL
);